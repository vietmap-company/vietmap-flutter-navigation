# Enable AndroidX and Jetifier for AndroidX dependencies
android.useAndroidX=true
android.enableJetifier=true

# Kotlin daemon and compiler settings
kotlin.code.style=official
kotlin.incremental=true

# Increase Gradle daemon heap and include useful OOM diagnostics
